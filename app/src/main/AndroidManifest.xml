<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="swufe.cc.www.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FrameActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RateCalcActivity"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog" />
        <activity android:name=".MyList2Activity" />
        <activity android:name=".MyListActivity"></activity>
        <activity android:name=".RateListActivity"></activity>
        <activity android:name=".Rate2Activity" />
        <activity android:name=".RateActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScoreActivity" />
        <activity android:name=".HelloActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>